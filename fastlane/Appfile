app_identifier("com.kursi.android")
json_key_file(ENV["PLAY_STORE_KEY_FILE_PATH"] || "secrets/play-service-account.json")

for_platform :ios do
  app_identifier("com.kursi.ios")
  apple_id(ENV["APPLE_ID"] || "")
  itc_team_id(ENV["ITC_TEAM_ID"] || "")
  team_id(ENV["TEAM_ID"] || "")
end
